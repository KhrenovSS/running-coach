# State Assessor → AthleteState
# Этап 0 — скелет модуля коуча (Coach module skeleton). Реализация — на последующих этапах.

def assess_state(user_id: int, db=None):
    """Собрать AthleteState из скиллов (Assemble AthleteState from skills)."""
    raise NotImplementedError("Этап 1")
