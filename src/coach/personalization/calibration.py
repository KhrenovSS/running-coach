# Персонализация: calibration
# Этап 0 — скелет модуля коуча (Coach module skeleton). Реализация — на последующих этапах.

"""Калибровка: residual → EWMA-обновление UserModel (ограниченно), запись PredictionLog."""
