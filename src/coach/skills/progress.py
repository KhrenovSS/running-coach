# Скилл: progress
# Этап 0 — скелет модуля коуча (Coach module skeleton). Реализация — на последующих этапах.

def evaluate(user_id: int, db=None) -> 'SkillResult':
    """Скилл прогресса: тренды VO2max/LTHR/stamina (analytics_helpers)."""
    raise NotImplementedError("Этап 0: заглушка — реализуется позже")

# from src.coach.contracts import SkillResult
