# LLM client (обёртка API, низкая температура)
# Этап 0 — скелет модуля коуча (Coach module skeleton). Реализация — на последующих этапах.

"""Обёртка над LLM API. LLM — только интерфейс/объяснение, НИКОГДА не меняет числа."""
