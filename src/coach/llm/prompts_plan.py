# Промпт недельного плана (Weekly plan prompt) — вынос из prompts.py (лимит ~400 строк/файл,
# 17.09.2026). Текст волатильных чисел не содержит: все числа недели — в week_targets контекста.
# (Plan prompt; every number lives in week_targets, never in the prompt.)

from __future__ import annotations

from src.coach.config import SAFETY_RUN_STREAK_MAX_DAYS

PLAN_PROMPT = (
    "Составь план тренировок на планируемую неделю. Все числа недели УЖЕ "
    "посчитаны в week_targets: target_km, потолки качества (quality_z4/z3_km_max), "
    "long_run_km_max/long_run_min_max, hard_days_max, фаза мезоцикла "
    "(build/deload), run_days_max/rest_days_min — НЕ выходи за них и НЕ пересчитывай. "
    "goal — подготовка к целевому старту (считает код): goal.phase = race_week — старт на день гонки "
    "ставит код (в weekly_plan его НЕ включай), остальные дни короткие лёгкие, качества нет, "
    "в message — настрой на старт без чисел; taper — объём уже снижен кодом, не больше одного "
    "короткого качественного дня, длительная короче обычной; build с peak_capped_by_date = true — "
    "честно скажи, что литературный пик к дате не набрать, цель — подойти свежим; maintenance — "
    "объём на потолке, растим качество и экономичность, не километры. Проза плана — мотивирующая, "
    "прогресс подопечного сравнивай с ним самим прошлым, не с нормативами. "
    "long_run_min_hint (если не null) — минуты длительной, соответствующие потолку км по твоему "
    "темпу: duration_min длительной не выше него (иначе код урежет). "
    "hard_days_max = 0 (quality_blocked_by_safety) — качественных дней в плане нет: ни tempo, "
    "ни interval, и в message не обещай темповую/интервалы. Допустимы короткие ускорения "
    "15–20 с (4–6 повторов, свободно) в конце ОДНОГО лёгкого дня — ТОЛЬКО как segments: "
    "warmup → work amount_kind=sec amount_value=20 repeat=4–6 target_zone=3 effort=«свободно» "
    "с recovery {duration_min: 1–2} (гайд 61: 45–60 с трусцой, обязательно) → cooldown; "
    "duration_min элемента = сумма сегментов с отдыхом (иначе код пересчитает); "
    "сам элемент — workout_type=easy, "
    "target_zone=2 (НЕ ставь зону 3–4 всему дню — safety срежет её, а ускорения пропадут). "
    "quality_allowed_from_days_ahead = N — интенсив закрыт только до этого дня: ОДИН качественный "
    "день (tempo) допустим ТОЛЬКО с for_days_ahead ≥ N, раньше — лёгкие; в message скажи, что "
    "темповая возвращается с этого дня. "
    "lthr_test_due = true — единственный качественный день недели станет полевым тестом ПАНО "
    "(30 мин ровно на максимуме; структуру и число ставит код): поставь ОДИН tempo-день там, где "
    "он уместен (свежесть, не перед длительной), в message объясни зачем тест и как его бежать. "
    "volume_held_by_safety — объём недели не растёт (цель = прошлая неделя), в message не обещай "
    "рост объёма. volume_growth_kept — интенсив закрыт только из-за распределения нагрузки: объём "
    "растёт по прогрессии, лечим темп лёгких дней, а не километры — так и скажи. "
    "Если в athlete_requests подопечный просил что-то (ускорения, темповые вставки, другой день) "
    "и план этого не даёт — одной фразой скажи, что откладываешь и почему (данные/safety), "
    "не замалчивай. "
    "availability — окно доступности подопечного (дни недели и отменённые даты): "
    "days_ahead_allowed его уже учитывает, других дней не назначай. "
    "Если в контексте есть athlete_message — это реплика подопечного, вызвавшая "
    "перепланирование: учти её. Говорит, что не сможет бегать в какие-то дни — заполни "
    "unavailable_days_ahead (сдвиги от «Сейчас», 0 = сегодня) и НЕ включай эти дни в "
    "weekly_plan; день снова свободен — available_again_days_ahead; постоянные дни для бега — "
    "available_weekdays. "
    "Заполни weekly_plan: только тренировочные дни, for_days_ahead ТОЛЬКО из "
    "days_ahead_allowed (сдвиг от текущего дня из «Сейчас»; 0 = сегодня, если он в "
    "списке), по одному элементу на день; пропущенные дни = отдых, rest-элементы не "
    "включай. Если plan_scope = rest_of_week — неделя уже частично выполнена "
    "(done_km/done_runs/done_quality учтены): распределяй ТОЛЬКО remaining_km, не больше "
    "remaining_run_days_max беговых и remaining_hard_days_max качественных дней, "
    "выполненные дни не переназначай. Частота растёт постепенно, а не сразу до "
    "ежедневного бега. Качественный день (tempo/interval) не ставь "
    f"{SAFETY_RUN_STREAK_MAX_DAYS + 1}-м беговым днём подряд — утром safety срежет его до лёгкого "
    "(правило 22); перед качеством — день отдыха или лёгкий после отдыха. "
    "Распределение — по 80/20 и структуре недели из method_guides "
    "(лёгкие дни вокруг качественных, одна длительная). Учти сверку прошлой "
    "недели (week_plan_review) и carry_forward из recent_reviews. В message — "
    "коротко логика недели, БЕЗ чисел тренировок (числа отрендерит карточка); если в контексте "
    "есть week_report — одной фразой скажи, что меняется относительно прошедшей недели и почему "
    "(направление подготовки по next_week и concerns). proposal=null, assessment=null.")
